# おりがみナビ / Origami Navi

折り紙の折り方を3Dで工程ナビゲーションするスマホ+PC向けWebアプリ(折り紙版Google Maps)。
キャッチコピーは「折り方が、3Dで動く。/ Every fold, in 3D.」
(2026-08-10 に「折り紙、道順で。」から変更。道順の比喩だけでは何ができるアプリか
伝わらないため、3Dで一工程ずつ再生できることを前に出した)。
Vite + React + TypeScript + three.js。リモート: https://github.com/yoshihirohakuno/Origami3DnavigationApp

## 現在の状態

**鶴は完成形まで実装済み(15工程)。** 折りすじ×4(折って開く)→ 正方基本形(中線3本の連鎖回転。
対角線は面内で平らのまま=本物と同じ)→ 花弁折り前後(全側点タック済み)→ 首・尾の中割り折り
(ほぼ180°で立て、NECK_SPLAY=32°の[11,13]軸=視線方向軸の面内回転で左右対称に上向きへ開く)→
頭の中割り折り(くちばし)→ 羽(WING_ANGLE=72°で浅いV字に開く)。cameraAngle: 22 でほぼ正面から。
頭は首の細い三角(先端角±22.5°)を両縁のタック線6-16/6-12上の折り点18-19を軸に中割りして作る。
折り線の端点14/15/17/18/19は最初の折りすじ工程から展開図に埋め込み、以降全工程の moving に追加済み。
一覧カードの完成形は工程データから生成する(2026-08-10 に手描きSVG CraneFinalPreview を削除。
手描きは羽・首の形が実際の折りと違っていた。立体なのでナビ画面と同じ視点から投影される)。
紙色は表=薄い赤/裏=白。
**2026-08-13 原典 traditional/crane と照合: 段階Aの工程相違あり。**
おりがみくらぶの見本は ❶三角に折る→❷さらに半分→❸袋を開いて正方形→
❹裏側も袋を開く→❺〜❻向きを整える、という実折りの正方基本形ルート。
現行モデルは、対角線2本と十字2本を先にすべて折って戻してから、1工程で正方基本形へ
畳む。幾何的には同じ基本形に到達するが、`docs/verify-models.md` の基準では
工程順・どの層を折るかが原典と違うため「一致」ではない。原典準拠に直すなら、
三角2回から袋を開く工程を、正方基本形の連鎖回転ではなく実工程として再設計すること。
補足: Navigator は ResizeObserver で canvas のサイズ変化を監視(完成時に「完了を記録」ボタンが
出てウィンドウリサイズ無しに canvas が縮むと、以前は完成形が隅にズレて表示されていた不具合を修正)。

**手裏剣はおりがみくらぶの折り図に忠実な9工程で実装済み(v2)。** ❶観音折り→❷半分→
❸端を斜め45°(2枚は鏡写し)→❹端の帯を❸と平行な折り線で折り返して翼に(ユニットは翼が
斜めにずれた六角Z形)→❺朱を裏返して90°回し中央へ→❻藍を上にかさねる(藍の翼=ポケットが上)→
❼朱の翼を❹の線で開いて藍の帯を巻き込み閉じ直す(角度344°の1回転オペ、新しい折り線なし)→
❽全体裏返し→❾藍も同様で完成(対角同色・風車状の4尖り、中央で帯が編み込みに見える)。
白面を上に始め観音折りで色が出る(折り図と同じ)。展開図=0.5刻み5×5格子+対角線。
幾何は tools/solve-shuriken.mjs で厳密検証。エンジン拡張: FoldOp.spinZ(面内回転)。
多層平畳みの層順テクニック(README「2枚組みの表現」参照): 残差角の階段/軸上の紙はしを
movingから除外/非隣接層で共有される折り目縁の頂点複製(❷中心線・❹折り線)+z補正は
回転完了後に実施(回転前に持ち上げると蝶番が浮いて面がねじれる)。
差し込みの direction は軸の向きで互い違い(❼: T=+1/B=-1、❾: T=-1/B=+1、実測で決定)。
既知の制約: 多層の差し込みで完成形の中央にわずかな白のぞきが残る(180°未満で畳む
エンジンの層順の限界)。実用上は許容とし、以降は他作品のクオリティ向上を優先する。

**他作品のクオリティ向上(品質パス)進行中。** 各作品の完成形所見(2026-07-14 時点):
鶴◎/コップ◎ は良好。
**2026-08-13: 既存20作品の原典照合を完了。** 詳細は `docs/model-verification-report.md`。
同日に新規作品 `envelope`(ふうとう、4工程、原典未照合)と
`wallet`(さいふ、4工程、原典未照合)を追加し、総数は22作品。
一致: tulip/dog/cat/fox/rabbit/panda/bear/chick/whale(色は決定済み例外)/penguin/yacht/
ship/rocket/helmet/shuriken/heart/cup/square-base(単体原典なし、現行鶴の中間形と一致)。
段階Aの相違が残る作品: **crane**(原典は三角2回→袋開き、現行は折りすじ先行collapse)、
**box**(原典は「はこ1」、現行は3工程トレイ)。次に直すならこの2つを優先する。
- **コップ(cup): 原典 fun/cup と照合済み(2026-08-13)。**
  見本❶〜❻は、半分折り→左右の角を反対側へ→手前/奥のフタを別々に折る→
  口を開く、で現行6工程と対応する。工程2・3の折り線は `s=2-√2` / `√2-1` /
  `3-2√2` の厳密値で、角が反対側の斜め辺へ届く構成。完成図の外接比は
  line 183/137=1.336、color 166/122=1.361。アプリは立体投影なので単純な
  平面比比較はしないが、口が開いた台形コップとして一致。コード変更なし。
- **箱(box): 原典 traditional/box と相違あり(2026-08-13、段階A)。**
  原典は7パネルの「はこ1」(対角線の折りすじ→四すみを中心へ→左右/上下を
  中心へ折る→左右を開いて立体化)で、現行 `src/models/box.ts` の3工程トレイ
  (中央底+4壁を直接90°に立てる)とは工程順・折り線・折る層が別物。
  原典準拠にするには、4x4格子+対角線の展開図から作り直す必要がある。
  **2026-08-13 実装スパイク**: 4x4格子を各小正方形の中心で三角分割し、
  対角線・x/y=±0.5・x±y=±1をすべて面の辺にしたモデルを試したが、共有頂点のまま
  四すみを中心へ折ってから箱へ開くと、角の多層部分が互いに引っ張り合って完成形が
  箱として読めない破綻になった。原典準拠版は、各工程で同じ紙の角を共有する層と、
  最後に壁ごとに独立して立つ層を分けるため、少なくとも角・壁まわりの複製頂点と
  z補正を設計してから置き換えること。壊れた4x4共有格子案は採用せず、現行安定版を
  残している。詳細は `docs/model-verification-report.md`。
- **犬(いぬのかお): 折り図に忠実な6工程に作り直し(2026-08-03)。**
  easy/animal-face/dogfase の❶〜❺に対応。旧版は折り図を見ずに作っていたため
  ①耳の折り線が (±0.42,0) 始まりで頭が広く耳が小さい ②「折り目をつけて戻す」
  工程が無い ③最後が「手前を折り上げ+奥を裏へ山折り」で原典と違う、の3点が
  ずれていた。折り図から実測して、耳の折り線を上辺の (∓0.22,0) から斜辺上の
  (∓0.67,∓0.33) へ(耳先は (∓0.458,-0.742)=斜辺の外へ垂れるたれ耳)、
  マズルは ❹手前の1枚を y=-0.72 で折り上げ(白が出る)→❺奥の1枚を y=-0.75 で
  折り上げて重ねる(表の色。白は先だけのぞく=完成図の鼻先)に直した。
  層順: 耳は残差角の階段(奥層168°/手前層174°)で足りるが、マズルは
  奥層のヒンジ自体が手前層より奥にあるため階段では前後が途中で逆転する。
  マズル面専用の複製頂点(19〜21)を足して面ごと z 補正した。
- **ひよこ(chick): ことりと差し替えて新規追加(2026-08-10、全7工程)。**
  ことり(bird.ts)はおりがみくらぶに原典がないアプリ独自設計だったため、
  原典のある https://www.origami-club.com/easy/animal/chick/zu.html に差し替えた
  (bird.ts は削除。App.tsx / Editor.tsx の参照も置き換え)。
  工程: ❶縦の折り目(折って戻す。原典は「たてよこ」だが横は❹の折り線そのもの)→
  ❷左の角を中心までの1/3の縦線で内へ→❸折り返してくちばし→❹上半分を手前へ半分→
  ❺下の先をうちがわに(前後2枚)→❻むきをかえる(面内回転)。
  幾何(すべて実測と一致):
  ・❷の折り線 x=-2/3。左の角 (-1,0) はちょうど (-1/3,0) へ(実測 -0.667 / -0.333)
  ・❸の折り線は折り後 x=-7/12、展開図では x=-3/4(❷の折り線で鏡映)。
    くちばしの先は **x=-5/6**(折り図の実測 -0.83。体の左のふち -2/3 より 1/6 外)
  ・❺の折り線 (-0.14,-0.86)-(0.24,-0.76)。どちらもひし形の下の辺の上に乗り、
    下の角 (0,-1) だけが線より外なので前後2枚を1本の折りで回せる
    (実測: 角が (-0.087,-0.671) へ、前後どちらも z<0)
  ・❻は `spinZ: -14`(折り図❼のできあがりの傾きの実測 -14.3°)
  色: `sheetColors` は紙の意味どおり、表=黄 #e8b93c / 裏=白。白面スタートは
  `faces: backSideUp(F.map(orient))` のように面の初期向きで表す。front/back の
  反転で表現すると途中工程で表裏が破綻するため禁止。
  層: ❹で手前へ回る上半分が常に手前なので z 補正は不要。
  ハマりどころ: 頂点の式を `[T1X, T1X-1]` と書いてしまい ❺の折り線が下の角を
  通ってしまった(折りが効かず角が動かない)。下の辺は **y=-x-1** なので
  `[T1X, -T1X-1]`。ふちの式は「どの辺に乗るか」を式ごとに確かめること。
- **ねこのかお(cat): 折り図に忠実な7工程で完成(2026-07-16)。** おりがみくらぶ
  (easy/animal-face/cat)の❶〜❺+顔描きに対応: 半分折り(山折りで色の三角)→縦の折り目
  (折って開く、鶴の unfold パターン)→両耳(折り線があご先(0,0)=中心から出るのが要点。
  これであごがとがったV字になり、あご→耳先の斜めの縁が顔に出る=完成図と同じ)→
  てっぺんを手前へ折り下げ→うらがえす(assemble 180°、軸は x=0 上の頂点 [12,13])。
  完成面は裏側なので折り込みが見えず、耳も表の色で見える。
  紙の角と折り返し縁は層ごとに頂点複製+折り後に z 補正(shuriken のパターン)。
- **ねこ・きつね: 折り線の角度を実測で修正(2026-08-09)。** 完成形の縦横比が
  折り図(ねこ 1.21 / きつね 1.06)に対して 1.78 / 1.46 の横長だった。
  ねこ: 耳の折り線の外端 (∓0.68,±0.32)→(∓0.575,±0.425)、てっぺんの折り線 y=0.72→0.785。
  きつね: 鼻先の折り線 y=0.4→0.5(**先が下のふちの中央にちょうど届く**のが原典の目印。
  旧版は (0,-0.2) まではみ出していた)、耳の折り線は鼻先フラップの縁(45°)と平行で
  D=0.04 外側(実測 0.037)。耳先 (±0.04,0.96)、耳のあいだに幅 2D の切れこみ。
- **きつねのかお(fox)・うさぎのかお(rabbit): 新規追加(2026-07-16)。** どちらも
  おりがみくらぶ easy/animal-face 基準で、ねこの 2層+事前分割+頂点複製+z補正
  パターンを踏襲。きつね: 6工程(てっぺんを折り下げて鼻先が下にはみ出す→両耳同時。
  耳の折り線は鼻先フラップの縁と平行で交差回避)。うさぎ: **原典の帯を入れて作り直し(2026-08-04、全7工程)**。
  旧版は原典❸「下端の帯を折り上げる」を省略していたため、耳が「あご先から出る
  三角」になっていた。原典の耳は**幅0.155の細い帯2本**で、帯の外角がそのまま
  耳の先になる。帯(y=B=0.155)を折り上げると紙の下の角 (∓1,0) が (∓1,0.31) へ来て
  斜辺の外へはみ出し、これをあご先 (0,B) から45°で折り上げると耳先が
  (∓0.155,1.155) に立つ(折り図❺のピクセル実測と一致)。耳の折り線は帯の中も
  通るので、展開図では (0,B)→(∓0.155,0) と「くの字」に埋め込む。
  副産物として、耳を折った後の胴が**厳密な正方形のひし形**((0,0.155)/(0,1)/
  (∓0.4225,0.5775))になり、折り図の形と一致する。ライブラリは10作品(フッターの作品数は MODELS.length
  から自動表示に変更)。
  **2026-08-09: 保留していた❼「下の先をうしろに おる」を実装して全8工程で完結。**
  折り線は **y=2B=0.31**(折り返した帯のふち=耳の下端の角を通る線)。実測は完成図❽
  から 0.313・折り図❼の破線から 0.342 で、厳密値 2B を採用(下辺の幅 2B が耳2本ぶんと
  一致する)。あご先には8層が集まるが、**どの層も折り線より下にあるのは「あご先の
  1頂点」だけ**(全層が「あご先を頂点・底辺が折り線」の三角形になる)ので、
  `moving:[4,17]` の1本の折りで全層をまとめて回せた。層ごとの折り線の端点は
  展開図では別の位置になる(耳の層は帯の折り線上の (∓B,B)。❹の45°折りで (0,2B) へ回る)。
  ハマりどころ2点:
  ①**「折って開く」工程の戻しの符号**。❷a(valley)の自動符号が -1 なのに戻しも
    `direction:-1` だったため 178+178=356°=+4° 傾いたまま残り、❸の軸が z 方向に
    傾いて帯が奥へ回っていた(うらがえした後に帯の白が顔の前に出る原因)。
    戻しは +1。**auto 符号を実測してから戻しの符号を決めること**(box の
    direction は絶対指定、と同じ話)。これを直すと層順が変わるので、❹の残差角も
    上層168°/下層174°へ入れ替えた(❺で層順が反転するため、うらがえした後に
    表の色を見せる下層の耳を手前にする)。
  ②**平面でない四角形は左右で対角線がそろわない**。❸と❹で別々に回る耳の帯は
    四角形が平面でなくなり、`orient` の頂点順反転で三角形分割の対角線が左右で
    非対称になって片側だけ下の層の白が出た。鏡写しの対角線で明示的に2枚に割って解決。
  **❻も原典どおりに(2026-08-09)**: てっぺんには胴2層+❹で内側へ倒れた耳の三角の
  先2層=4層が集まる。胴の2層だけ折っていたため、耳の帯より外へ三角の先が幅0.045
  はみ出して裏の白が細く見えていた。三角の先は耳の帯と頂点を共有するので、
  三角の面だけの複製(38〜41)を作って❻の moving に入れる(共有したままだと
  耳の帯まで回って耳がつぶれる)。折り線の端点も層ごとに別頂点で、耳の三角側は
  展開図では x=∓0.645 の縦線(❹の45°折りで胴の折り線 11-13 に重なる)。
  なお三角の先は❺で奥へ回っているので❻の回転では手前に出てしまう(実測 z=+0.096)。
  複製頂点なので面ごと z=-0.16 沈めて解決(表裏マップが全面「表」になるのを確認)。
- **ぱんだのかお(panda): 原典どおりに作り直し(2026-08-04、全9工程)。**
  旧版は「てっぺんを後ろへ→上の両角を手前へ」という1層の折りで耳の見た目だけ
  似せた独自手順だった。原典は ❷左右の角を中央へ折って裏の色を出す →
  ❸上半分を後ろへ → ❹左右を後ろへ、で**残った三角が耳**になる。
  ・❷の折り線は x=∓0.6(角が (∓0.2,0) に来る。折り図❸の灰色の三角の内側の先と一致)
  ・❸は y=0 で上半分をまるごと後ろへ(折り図❹の「幅1.2・縦の辺0.4・下は45°」と一致)
  ・❹の折り線は x=∓0.477(折り図❺の輪郭 幅106px/縦の辺57px/高さ111px から逆算)。
    残る耳は三角 (∓0.477,0)-(∓0.2,0)-(∓0.477,∓0.277)
  ・❺は y=-0.72 で2枚まとめて折り上げ、❻は y=-0.585 で手前の1枚を中へ。
    -0.585 は「折り込んだ先がちょうど下のふち(y=-0.72)に重なる」位置=原典の目印
  色の出方: ❺は2枚重ねなので実物では奥の層が手前に来る。その層は表(生成り)を
  見せるので折り図❻でも鼻はまだ白く、❻でその1枚を中へ折り込むと下から裏(朱)の
  鼻が出る。エンジンでは残差角の差だけでは足りず(ヒンジが❸で後ろへ回っているため)、
  帯の面専用の複製頂点(32〜34)を足して面ごと z 補正した(dog/bear と同じ)。
  旧版のハマりどころ(側面折りの軸に前工程で動いた頂点を使うと軸が傾く)は
  引き続き有効: ❹の軸は不動の 19/10・20/12 を使っている。
  **配色を原典の白黒に変更(2026-08-09)**: 表=生成り #f2ede3 / 裏=炭 #2a2a2f。
  それまで裏を朱にしていたが、ぱんだは白黒の作品なので原典から離れていた。
  真っ黒(背景の墨 #0e0f12)だと背景に溶けるので、わずかに明るい炭色にしている。
- **チューリップ(花): 原典どおりに作り直し(2026-08-09、全6工程)。**
  原典 easy/flowers/tulips の花の部分。旧版は3工程で、花びらを上へ折って先が
  上辺の外に少し出るだけ(先 (±0.40,0.78))だったが、原典の花びらは
  **中心を越えて反対側へ**折り、その先が左右のとがりになる。
  ・❸の折り線は原点から60°。紙のふちとの交点 ((√3-1)/2,(3-√3)/2)=(0.366,0.634)
  ・右の角 (1,0) は原点まわりに120°回って (-0.5,√3/2) へ(折り図の実測 (-0.484,0.888))
  ・❹は鏡写しで、後から折る左が右の上に重なる(残差角 174/166 対 176/168)
  ・❺は y=0.2 で下の先をうしろへ。原点に6層集まるが、どの層も折り線より下は
    原点1頂点だけなので `moving:[4,14]` の1本で回る(うさぎのあごと同じ形)
  ・❷「折り目をつけて戻す」を原典どおり追加。旧版の z 補正による層順は残差角へ置換
  (旧メモ)2026-07-16 の改善: 旧版は工程1が谷折りのため、動かない
  上半分は表(正しい色)のまま、谷折りで手前へ回った下半分は裏返って白が前面に出て
  中央が割れて見えた。工程1を山折り(下半分を奥へ)に変更し、動かない上半分が常に
  表を向くようにして解消。工程2の花びらも同様の理由で片方の層が裏を向く構造だった
  ため、表を向く側の面だけ頂点を複製し(12/14=1・6の複製、13/15=3・9の複製)、
  折った後に複製面をまとめて手前へ z 補正(shuriken.ts のパターン)。
  実装時のハマりどころ: ①複製頂点の座標コピーミス(9の複製のつもりが8の座標に
  なっていた)で片側だけ大きく破綻→②複製面の頂点順序を左右で揃えてしまい
  (本来は鏡映に合わせて逆順にすべき)片側だけ法線が反転→の2段階のバグを
  それぞれ実測(法線のz符号・面の高さ)で特定して修正。全工程で正しい朱色になり
  左右対称を確認済み。

- **ぺんぎん(penguin): 新規追加(2026-08-12、全9工程 = 原典8工程 + 折り目の戻し)。**
  原典 easy/sea/penguin。ひし形(角 N(0,1) E(1,0) S(0,-1) W(-1,0))で始める。
  ・❷ 下の角を **y=-F(F=0.2)** で折り上げる(実測 0.186〜0.211)。角の先は y=1-2F=0.6
  ・❸ その白い三角の先を **展開図の y=-3/4 の弦**(折り後は y=0.35)で下へ折り返す。
    弦の幅がちょうど三角の幅と一致するのが目印。先は y≈0.1(折り図❹の実測 0.136)
  ・❹ うらがえす(以降、上の層=五角形の裏(白)が正面)
  ・❺ 左右を **x=±(1-F)/2=±0.4** で内へ。下の2つの角が中心でぴったり出会う(実測 0.379)
  ・❻ **x=±0.2**(実測 0.195)で外へ折り返してはね。展開図では x=±0.6(❺の折り線で鏡映)
  ・❼ あたまの先を y=0.87 で下へ(先は y=0.74)
  ・❽ うらがえして目を描く。完成形の縦横比 1.12(折り図の完成図の実測 1.07)
  **❻の読み解きの鍵**: ❺で内へ入るフラップには**紙の左右の角 E/W が含まれる**ので、
  折ると角は中心を越えて x=∓0.2 まで来る。これを❻で外へ回すと **x=±0.6 まで
  張り出す**(折り図❼のすその形。実測 0.62)。「フラップは下のふちの角まで」と
  読むとこの張り出しが出ずに胴のふちとぴったりになってしまい、折り図と合わない。
  色: 色のついた面を上にして始め、❽のあと上の層の表=あたま、下の層の裏=おなか。
  おなかのV字のふちは「二つの層のさかいめ」で、折り図❻の青いV字は
  「上の層のフラップのうち、下の層のフラップに隠れていない帯」。
  **ハマりどころ(既知の再発)**: ❺❻の軸に❷で持ち上がった頂点(19/17)を使うと
  軸が z 方向に 4° 傾き、フラップが手前ではなく**奥へ回って全部白く見える**。
  軸は❷で動かない頂点だけ(4-14 / 6-12)で取ること。
- **ろけっと(rocket): 新規追加(2026-08-12、全8工程 = 原典7工程 + 折り目の戻し)。**
  原典 easy/vehicle/rocket。紙は [-1,1] の正方形。
  ・❷ 上のふちを中心の折り目へ(**y=0.5**)。折り下げた帯は y=0〜0.5 を覆う
  ・❸ うらがえす → ❹ 上の両角を中心へ(折り線 **(0,0.5)→(±1,-0.5)** の45°。
    角 (±1,0.5) がちょうど中心線上の (0,-0.5) に来る。屋根は幅1・高さ0.5の三角)
  ・❺ 左右を中心へ(**x=±0.5**、実測 0.500/0.536)。ふちが中心でぴったり出会う
  ・❻ 下の左右を外へ(**x=±1/3**、実測 0.327)。❺で内へ入ったふち(折り後 x=0)が
    **x=±2/3** まで出て尾翼になる(完成図の実測 0.664)。折り図の破線が y=-1〜-1/6 なのは
    そのフラップの上のふちが x=1/3 で y=-1/6 になるから = フラップの範囲そのもの
  ・展開図では先に折った層の折り線を鏡映して埋める: 帯の❹は (0,0.5)→(±0.5,1)、
    ❺のフラップ側は y=0 の (±0.5,0)-(±1,0)、❻は❺の折り線で鏡映して x=±2/3
  ・完成形の縦横比 0.889(折り図の完成図の実測 0.892)
  **ハマりどころ**: ❹で回る「帯のフラップ」と「主の層のフラップ」は同じ角度だと
  z 差が 0.001 しかなく、主の層の白が前に出て折り図❻の「まん中が緑・外側が白」に
  ならない。帯側の頂点を複製(26/27)して**残差角を 176°/173° にずらす**と正しく出る。
  尾翼も❹のフラップと (±1,-0.5) を共有すると引きずられるので複製(24/25)している。
- **ふね(ship): 新規追加(2026-08-12、全3工程)。2つめの「のりもの」。**
  原典 easy/vehicle/ship。紙は [-1,1] の正方形。
  ・❶ **y=-1/3** で下から 1/3 を手前へ折り上げる(折り図に「1/3」の目盛りがある)。
    折り上げた帯は y=-1/3〜+1/3 を覆い、残りの白い部分とちょうど同じ幅になる
  ・❷ **(1/3,1)→(-1,-1/3)** の45°線で、白い層の左上をうしろへ(船室の前の傾き)。
    折り図の破線は帯のふちで止まっているが、これは帯の下の紙が見えないだけ。
    折るのは白い層だけで、帯は動かさない
  ・❸ **(-1,1/3)→(-1/3,-1/3)** の45°線(折り後)で左下をうしろへ(へさき)。
    帯とその下に残る白い層をまとめて折る。展開図では帯の分が (-1,-1)→(-1/3,-1/3)
  **ハマりどころ**: ❷でうしろへ回る三角も、❸の折り線で分割しておくこと。
  分割しないと、その三角の角が❸で切り落としたへさきの外へはみ出して裏の色で見える。
  ❷と❸の折り線は直交しているので、❸の折り線は❷で鏡映しても同じ直線に重なる
  (展開図の分割線は (-1,1/3)-(-2/3,0))。
  色: `sheetColors` は紙の意味どおり、表=橙 #ffa800 / 裏=白。船室は白のまま。
  白面スタートは面の初期向きで表し、front/back の反転で表現しない。
- **ヨット(yacht): 新規追加(2026-08-12、全3工程)。アプリ初の「のりもの」。**
  原典 easy/vehicle/yacht。❶対角線で半分 → ❷**上の1枚だけ**を TR の角の
  二等分線で左へ(右のふちを斜辺に合わせる)→ ❸下を折り上げて船体。
  ・❷の折り線が下のふちと交わる x は **K=3-2√2≈0.172**(実測 0.16)。
    折られた角はちょうど斜辺の上の (1-√2,1-√2) に来るので、折り図❸の輪郭が
    三角形のままではみ出さない
  ・❸の折り線は斜辺の (-0.58,-0.58) から右のふちの (1,-0.81)(破線の実測)
  ・上の層だけを折る工程があるので、上の層の折り線は展開図では
    **対角線 y=x で鏡映した位置**(x と y の入れ替え)に埋め込む。
    ❸の折り線は❷のフラップの上でさらに折れるので、展開図では「くの字」になる
    (フラップ側の端点は❷の折り線で鏡映して逆算)
  ・色: `sheetColors` は紙の意味どおり、表=藍 #41b8d4 / 裏=白。白面スタートは面の
    初期向きで表す。❷で上の1枚が
    どくと下から白い裏面が出て帆になる。❸は約180°なので z の前後が入れ替わり、
    下の層が船体の表に出る(残差角のままで層順が正しく出る)
  ・折り図❹のできあがりは船底が水平になるよう 7.3° 回して描かれているだけなので、
    アプリの完成形は少し右下がりに傾く(折り自体は❸で終わり。回す工程は足していない)
- **くじら(whale): 頭と尾が逆だったのを修正(2026-08-03)。** 旧版は凧折りの
  ヒンジを左の角に置き「とがった左=頭 / 太い右=尾」としていたが、原典
  (easy/sea/whale)はヒンジが**右の角**で、できた鋭い先が**尾**、折り残した
  鈍角の左が**頭**。くじらの頭が丸く尾びれが細いのはこのため。全6工程に
  作り直し、原典❸の「頭の先を折る」(x=-1/√2 で折ると先が凧のとがり先
  (-0.414,0) にちょうど重なる)も追加した。かぶせ折りの折り線は本体だけでなく
  凧のフラップにも入れる必要がある(フラップ側は本体側の線を凧の折り線で
  鏡映した位置。畳むと1本の軸に重なる)。
  作品としては動物の顔シリーズ以外で初、かぶせ折り(outside-reverse)の実装例。
  横半分の折り目→凧折り(厳密値 K=√2-1 で辺が折り目にぴったり重なる)→頭の先→
  半分に折って横長の体→尾をかぶせ折りで上へ。配色は藍系(sheetColors)。
  ハマりどころ2点(いずれも実測で特定):
  ①かぶせ折りの軸を縦(x一定)にすると回転が水平面内になり、尾が上へ立たず左へ
  折り返されるだけになる。軸を斜め(背中側(0.85,0)-腹側(0.62,-0.38))にして解決。
  ②凧折り・半分折りを谷折りにすると、手前へ回った面が裏返って裏の白が前面に出る
  (tulip と同じ)。両方とも山折り(奥へ)にし、動かない側が常に表を向くようにして解決。
- **くまのかお(bear): 原典どおりに作り直し(2026-08-04、全9工程)。**
  旧版はぱんだ型(半分に折らず1枚のまま角を折り込む)6工程で、完成形は似ていたが
  折り手順が原典と別物だった。原典は「半分に折る2層構造+中心を越えるフラップ」。
  ・❸の折り線は上辺の (∓0.296,0) から斜辺上の (∓0.435,-0.565) へ(ほぼ縦)。
    角 (∓1,0) は **中心を越えて (±0.328,-0.327)** まで回る。❹の折り図で内部に
    長い斜め線が交差して見えるのはこれ
  ・❹はその交差した先を外へ折り返して大きな三角の耳にする。折った後の状態での
    折り線 (-0.122,-0.091)-(-0.234,-0.502) で、先が **(-0.628,-0.066)** に来る
    (折り図❺の実測 (-0.639,-0.086) と一致)
  ・❺で耳の先を内へ折ってまるくする(先が (-0.389,-0.131) へ)。❺は手前へ折るので、
    ❻うらがえす の後は耳の裏側に隠れる=完成形の耳は表の色一色になる
  ・❼下の先を折り上げてマズル(裏の生成り)、❽でマズルの先とあごの先を後ろへ
  展開図への戻し方: ❹の折り線は❸の折り線で1回、❺の折り線は❹→❸で2回、
  鏡映を逆にたどる(bird.ts / helmet.ts と同じ手法)。
  旧版の「うさぎ型では幅:高さ≒2:1 の横長にしかならない」という所見は、
  **耳を1回の折りで作る場合**の話だった。原典は❸❹の2段階で耳を作るので、
  耳先は原点まわりの単位円に縛られず、縦長の頭+外へ張り出す耳が両立する。
  (=z補正する頂点は、その面だけが持つ複製頂点にすること)
- **ハート: 原典❺❻を追加(2026-08-09、全7工程)。** 保留していた「左右の角と
  山の先をうしろへ折って丸める」を実測して実装。❺の折り線は折り後の縦線
  x=∓0.475(実測 ∓0.479/∓0.472)、❻は折り後の水平線 y=0.67(実測 0.668)。
  ❻は翼が❹で回っているので、展開図では縦線 x=∓0.92(❹の折り線で鏡映して逆算)。
  どちらも「線より外は角1点だけ」なので、本体・❷のフラップ・翼の3層を
  同じ折り線で切って1本の折りで回せる。完成形の肩と山がまるくなり、原典の
  できあがり図と一致した。
- **くま: あごの折り線を修正(2026-08-09)。** ❽のあご側を y=-0.804 にしていたため
  マズルの下に地の色の帯が残り、完成形が 1.32:1(折り図は 1.56:1)の縦長だった。
  **あご側は❼(マズル)と同じ y=-0.647**。前後の層の先を同じ線で互い違いに折るのが
  正しく、マズルの白が完成形の一番下まで届く(折り図の❽・完成図と同じ)。
  修正後 1.64:1。
- **兜(helmet): 原典7工程に戻した(2026-08-03)。** 旧版はつのを1回の折りに、
  帯も1回にまとめた4工程だったため、つのが低く小さく(先が (∓0.5,-0.3))
  兜に見えにくかった。原典どおり ❸「手前の先をひし形の横の対角線で上へ折り上げる」
  →❹「その先を斜め外へ開く」の2段階に戻し、つのの先が (∓0.409,-0.210) で
  ひし形の斜辺の外へ張り出すようにした(実測で一致確認)。帯も
  ❺折り上げ→❻対角線で折り返し→❼奥の先を中へ、の3工程に戻した。
  ❹の折り線は「折り上げた状態の (0,-0.5)-(∓0.17,-0.17)」を展開図へ戻して
  (∓0.5,0)-(∓0.83,∓0.17) として埋め込む(2段階の鏡映を逆算)。
  ハマりどころ: ❻の折り返しは山谷の自動判定が valley と同符号になるので
  direction:-1 を明示する(帯の白が見えなくなるのを実測で発見)。
  旧版のノウハウ(❷は谷折りだとフラップが裏返る等)は、つのを手前に出す必要から
  ❷を谷折りに戻し、代わりに奥層の残差角を大きく(168°/174°)して層順を作る方式にした。
  ノウハウ: **折り後の位置から逆算して展開図に折り線を埋め込む**手法(bird.ts と同じ
  考え方)。多段の折りでは、鏡映を折りの回数だけ逆にたどれば展開図の座標が出る。
  検証(実測): ❷で4つの角が (0,-1) に一致 → ❸で全て (0,0) に一致 → ❹でつのが
  (∓0.409,-0.210)、奥層が (∓0.413,-0.215) で手前(z が大きい)。

## 完成形の縦横比を折り図と突き合わせる(2026-08-09 に導入した点検手順)

折り図の**完成図のパネルをピクセル実測して幅:高さを出し**、アプリの完成形の
バウンディングボックスと比べると、折り線の角度のずれが一目でわかる。
`window.__origami.setT(99)` → `getPositions()` の x/y の最大最小で比較する。
実測値(おりがみくらぶ): ねこ 1.21 / きつね 1.06 / 犬 1.78 / くま 1.56。
この方法でねこ(1.78→1.21)ときつね(1.46→1.10)のずれを見つけて直した。
紙色の付いた図なら「不透明かつ有彩色」の画素で紙の領域を判定できる
(ぱんだのように白黒の図はこの判定が効かないので別途)。

## 辺の長さで「取り残された頂点」を自動検出(2026-08-14 に導入)

折り紙は**等長変形**なので、面の各辺の長さは展開図から一生変わらない。
`computeFoldState` を工程ごとに回して、面の全辺について「展開図の長さ」と
「その時刻の長さ」を比べると、**折り線の向こう側にあるのに `moving` へ入れ忘れた頂点**が
一発で出る(その頂点だけ取り残されて、辺が伸び縮みする)。ブラウザのコンソールで:

```js
const { computeFoldState } = await import('/src/engine/fold.ts');
const { envelopeModel: m } = await import('/src/models/envelope.ts');
// 面の各辺の展開図長 → 各 t での長さと比較(整数 t = 静止状態だけ見るとノイズが減る)
```

判定の目安(2026-08-14 の全23作品の実測):
- **0.06 未満は正常**。残差角(174〜178°)で層ごとに回転量が違うぶんの誤差。
- **0.08 以上の静止状態は要調査**。この方法で てがみ の❶(0.667。頂点27/28 の入れ忘れ)を
  検出して直した。
- 残る検出:**ひよこ**(❶の縦折り t=1 で辺 1-7 が 1.667→0.335。体の面 `[7,6,13,14,1]` と
  `[7,10,11,12,1]` が折り線 x=0 で分割されていない=折り目をつける工程で紙がねじれる)、
  **ねこ**(t=2 で辺 19-20 が 0.43→0.09)、**鶴**(中割り折りの工程。エンジンの
  reverse 折りは1頂点だけ回すので原理的に等長にならない=仕様)。
  ひよこ・ねこは未修正(層順の再確認が要るので別作業)。
- 箱・正方基本形は**工程の途中(t が非整数)だけ**大きく出る。90°の立体折りと
  連鎖回転の畳み込みなので、静止状態で戻っていれば問題ない。

## 完成形の「裏面が見えている割合」で全作品を自動点検(2026-08-10 に導入)

`window.__origami` から完成形の全面を取り、シルエット内を格子状にサンプルして
**最前面の面が裏向き(nz<0)である割合**を作品ごとに出す。層順の破綻(本来隠れる面が
手前に出て裏の色が細く見える)を数値で拾えるので、目視より確実。
面ごとの内訳も出すと、どの面が悪さをしているか一発でわかる。

2026-08-10 の測定結果(裏面%。設計どおりのものも含む):
- ひよこ97 / ハート100 / 手裏剣81 … 当時は「折り返した裏の色が作品の色」として
  front/back を反転していたが、2026-08-13 に修正。以後 `sheetColors` は必ず
  **front=紙の色面 / back=白い裏面**で定義する。ハート・ひよこ・ふね・ヨットの
  白面スタートは `faces` を反転して表し、手裏剣は途中工程の色を保つため
  `displaySideSwapFromStep: 8` で完成側だけ表示面を切り替える。
- コップ55(フタの三角) / 兜42(前立て) / くま18(マズル) / ぱんだ15(耳と鼻) /
  犬14(マズル) / 鶴20(羽の裏) … **設計どおり**
- 箱0 / きつね0 / 正方基本形1.1 / うさぎ1.4 / ねこ1.6 / チューリップ3.5 …
  残りは輪郭1セル(約2px)のはみ出しだけで実用上問題なし

この点検で見つけて直したもの:
- **ぱんだの黒い線**: ❸で後ろへ回るべき「❹の折り線より外・上」の面が、耳と頂点を
  共有するため z が混在し、耳のすぐ内側で手前に出ていた(左は裏=黒が細い線に見え、
  右は表=白なので目立たない、という非対称)。**面専用の複製頂点(35〜42)を作って
  ❹の後に z=-0.07 沈めて解決**。修正後は裏面が耳と鼻だけになり左右対称。
- **コップの胴が白かった**: ❶が谷折りで、手前へ回った下半分が裏返っていた
  (完成形の45%が裏の白)。**❶を山折りにして前後のフタ(❹❺)と❻で開く層を入れ替え**、
  外が紙の表の色・フタが白い三角の古典のコップの見た目になった。

- **てがみ(envelope): 原典へ作り直し(2026-08-13、全7工程)。** 原典 use/letter(伝承)。
  それまでの「ふうとう(4つの角を中心へ)」は原典のない独自設計だった(ことりと同じ状況)。
  ・❷ 下のふちを **y=-2/3** で折り上げ(折り図の「1/3」は下半分を3等分した目盛り)
  ・❸ 帯の下の両角を 45°(x+y=∓4/3)。角が帯のふち (∓2/3,-1/3) に着地
  ・❹ 左右を **x=±2/3** ・❺ 上の両角を上のふちの中点から 45° で中心線へ
  ・❻ **y=1/6** でさしこむ(先が帯の底 y=-2/3 まで入る)。
    **完成形の縦横比 1.60 が見本の完成図の実測 1.613 と一致**するのでこの位置で確定できる
  ハマりどころ3点(どれも完成形に白がのぞくかどうかを決める):
  ①❸は帯の角だけでなく**その下の紙も一緒に**折る(折り図❺で輪郭が細くなる)。
   下の紙側の折り線を入れないと共有頂点が引きずられて胴がねじれる
  ②❹で持ち上がった層のうち❻の折り線上に乗る面は、「折り線上だが z が持ち上がった頂点」を
   錨にしてしまい、さしこんだ屋根が前に出て白がのぞく。その面だけ複製頂点を持たせる
  ③❹(左右)は自動符号だとフラップが奥へ回るので `direction` を明示(右+1/左-1)
  **2026-08-14 修正: ❶の折り目で下半分が壊れていた。** `LOWER`(❶で折り上げる下半分)に
  頂点 27/28(❸の折り線・下の層の端、y=-1/3)が入っておらず、この2点だけ下に取り残されて
  左右のフラップが下へ垂れ、工程02(開いて戻す)の形が「両はしに足が生えた」ように見えていた。
  **折り線より向こう側にある頂点は1つ残らず moving に入れること**(下記の辺の長さ点検で検出)。
- **ぴあの(piano): さいふの差し替えとして新規追加(2026-08-13、全8工程)。**
  原典 easy/other/piano。さいふは原典が無く(つかう/ふぁっしょんを検索済み)、
  四隅の二重折りで完成形も破綻していたため、ユーザー判断で差し替えた
  (ことり→ひよこ、ふうとう→てがみ と同じ扱い)。
  ・❷ 左右のふちを **x=±3/4** で内へ(折り図の「1/4 ぐらい」は**半分の幅の1/4**。実測 0.25/0.26)
  ・❸ 左上の角を **(-3/4,0)→(0,1)**(❶の折り目の端どうしを結ぶ線)で折る。
    帯にもかかるので展開図では帯の分を鏡映して (-1,1/3) まで入れる
  ・❹ うらがえす → ❺ 下を **うしろへ** 折り上げ(y=-0.65、実測)。
    手前へ折ると帯が白い裏面を見せる。折り図❻は全面が紙の色なのでうしろが正しい
  ・完成形の縦横比 0.91(見本の完成図の実測 0.87)
  ハマりどころ2点: ①❷の残差角を深く(170°)しないと、❹でうらがえした後に帯と胴の z が
  拮抗して白い裏面がちらつく ②❺の折り線上にある帯のふち(11/15)は❷で持ち上がった z を
  持つので、moving に入れると z が反転して正面に出る。**軸上の紙はしとして moving から外す**
- **かめ(turtle): 新規追加(2026-08-14、全5工程)。かぶせ折りの2例目。**
  原典 easy/animal/turtle(作・図:新宮文明)。紙は [-1,1] の正方形、白い面を上に始める。
  ❶「/」の対角線に折り目 → ❷右上の角を**対角線と平行な線 x+y=C** で左下へ →
  ❸対角線で半分(左上を右下へかぶせる)→ ❹左の先をかぶせ折りして頭 →
  ❺(折り図)目と甲羅を描く=折りではないので工程にしない。
  ・**C=0.42 は目印のない実測値**。折り図には「てんせんで おる」としか書いておらず、
    1/2・二等分線などの目印がない。実測は ❷の破線 0.413/0.422、❸の緑の三角 0.435/0.396、
    ❹の右の短い辺と頂点 0.437/0.448 で、3パネルの平均を採った
  ・右上の角 (1,1) はこの線で **(C-1,C-1)=対角線上**へ倒れる(折り図❸の緑の三角の
    直角の角が対角線に乗るのと一致)。❸のあとの輪郭は
    (-1,-1)/(C/2,C/2)/(1,C-1)/(1,-1) で、折り図❹の実測(1単位≈91.5px)と一致
  ・**かぶせ折りの折り線は背の (-0.70,-0.70) から下のふちのまんなか (0,-1)**。
    実測は背側 (-0.686,-0.719)・ふち側 (-0.063,-1)。先 (-1,-1) は (-0.69,-0.28) へ回り、
    背より外へ出て頭になる。完成形の縦横比 1.405(折り図の完成図の実測 1.42)
  ・**頭が白いのはかぶせ折りで2枚とも裏返るから**。上の層(面0)は❸で持ち上がった
    頂点9(=左のふちの中点。❸で下のふちの中点8に重なる)を持つので、回したあとも
    下の層(面5)より手前に残り、白い面が頭として見える(くじらと違い色が正しく出る例)
  ハマりどころ:
  ①**❷のフラップの先(頂点1)を❸の moving に入れる**。この点は❷の残差で z>0 に
    浮いたまま❸の折り線(対角線)の上に乗るので、動かさないとフラップ2枚が
    左上の胴より手前に出て、背に沿って白い三角が見える。❸の回転に含めると
    z が反転して胴の下へ沈む
  ②❸の残差角は 177。174 にすると胴が内側へ 0.5% 縮んで、下のふちに沿って
    下の層の白が細くのぞく(裏面の割合が 0→0.7% に増える。実測で確認)
- **鶴・箱の原典との相違は「現行維持」で決定(2026-08-13、ユーザー判断)。**
  鶴: 原典 traditional/crane は「三角2回→袋開き」だが、アプリは折りすじ先行の collapse。
  同じ正方基本形に到達するのでルート差は明記して残す。箱: 原典 traditional/box は
  7工程の「はこ1」だが、アプリはふたなしのトレイ(伝承)。別作品である旨を明記して残す。
  **どちらも再検討しない。**

## 引き継ぎ

**セッションをまたぐときは `docs/handoff.md` を最初に読む。** 現在の状態・進行中の作業
(いまは かめ の新規追加が折り図の読みまで済み・コード未着手)・決定済み事項・保留を
1枚にまとめてある。作業を中断するときはこのファイルを更新すること。

## 折り図との照合(他エージェントへの作業指示)

**`docs/verify-models.md`** に、全作品を折り図(見本)と突き合わせて相違を直すための
手順書がある。実測の道具は **`tools/zu-measure.mjs`**(折り図の取得・アスキー俯瞰・
外接矩形・行ごとの色の区間・線の位置)。作業コピーは `tools/.zu/`(gitignore 済み)。
**鶴(traditional/crane)・コップ(fun/cup)・箱(traditional/box)は原典が存在する**
ことが 2026-08-12 に分かったので、この3つは未照合(「原典なし」と書いていたのは誤り)。

## 保留中の課題

- **次に足す候補の実測メモ(2026-08-12)。** 折り図は取得済みで、折り線の目印まで出してある。
  - **えんばん(easy/vehicle/ufo、全7工程)**: 紙は [-1,1] の正方形。折り図は白い面を上に
    見えるが、実装時の `sheetColors` は front=橙 / back=白で定義すること。
    ❷下半分を上へ折って 2:1 の長方形(裏の橙が出る)→❸**上の1枚だけ**その上半分を
    下へ折る(折り線は折り後の y=0.5。展開図では下の層の y=-0.5。これで見た目が
    白にもどり、輪郭は 2:1 のまま = 折り図❹と一致)→❹中線の中点 (0,0.5) から
    下の両角 (±1,0) への斜線で、帯の左右を下へ折り出す→❺上の両角を
    **x+y=±1.4** の45°線で内へ(実測: 上のふちの交点 ±0.382、横のふちの交点 0.327/0.35)
    →❻うらがえす(折り図❻の矢印はループ=うらがえし。キャプションは「てんせんで おる」
    だが図と完成図はうらがえしでないと合わない)→❼まどを描いて完成。
    **未解決は❹**。完成図の脚(白い三角)の見えている部分は
    **(-1,0)-(-0.375,0)-(-0.605,-0.287)** で、これは「中線と斜線ではさまれた三角
    (0,0.5)-(-1,0.5)-(-1,0) を斜線で折り返した形」とぴったり一致する(実測で確認)。
    ところが**その三角は中線側が折り目(ヒンジ)なので、そのまま回すと紙が切れる**。
    どの層をどう回しているのか(スワイベル的な動きか)が読めていないので保留。
  - **さかな2(easy/sea/fish2、全7工程)**: ❶たてよこの折り目→❷右の2つの角を中心へ→
    ❸左の2つの角を中心へ(**折り図❹の左半分が白いので、こちらはうしろへ折る読み**)→
    ❹左の角から右上のふちの**中点 (0.5,0.5)** への45°線(実測 -1/3 の傾き)で上を折り下げ→
    ❺その鏡写し→❻はんぶんに おる→❼うらがえす。
    **未解決**: ❺のパネルの輪郭が❹の結果と合わない(❹で上の三角を折り下げたなら
    輪郭の上辺は折り線になるはずが、図には (-0.30,0.73) に頂点が残り、
    底辺 (-1,0)-(0.43,0)・頂点 (-0.285,0.715) の白い45°三角が乗っている)。
    層の読み(どの層を折るか)が違う可能性が高いので、推測で作らないこと。


**折り図との差で未修正のもの(2026-08-03 の点検で洗い出し)。**
いずれも「見た目は近いが折り方の構造が原典と違う」もので、直すには作品の
作り直しが要る。優先度順:

- **コップ・ことり・箱・正方基本形はおりがみくらぶに対応する折り図がない。**
  コップと箱は伝承作品(コップは古典の厳密解、箱はふたなしのトレイ)、
  正方基本形は基本形。ことりだけはアプリ独自設計なので、伝承作品ではない旨の
  明示か差し替えを要相談(既出)。
- **(解決済み・ノウハウ)「折って開く」工程の戻しの符号(2026-08-09)。**
  うさぎで見つけたバグ:`type:'unfold', direction:-1` は、直前の valley の
  **自動符号が +1 のときだけ**正しく戻る。-1 のときは 178+178=356° になり、
  紙が +4° 傾いたまま残って以降の折りの軸が z 方向に傾く(層順が狂う原因)。
  **2026-08-09 に全作品を点検して修正済み。** 点検方法: 各作品で
  「折り目をつける工程の直前(t=k-1)」と「開いて戻した直後(t=k+1)」の全頂点位置を
  比べる(打ち消せていれば距離 0)。打ち消せていない作品はどれも距離 0.070
  (=4°傾き)で一目でわかった。cat/fox/rabbit/tulip/dog/panda(1つ目)/heart/bear を
  +1 に修正(修正後の距離は 0〜0.001)。
  whale と panda の2つ目(UPPER_HALF)は元から打ち消せていた(軸の向きの都合で
  auto 符号が +1 のため)。修正後も各作品の完成形・配色に破綻がないことを画面で確認。
  crane だけは `direction: fold.direction === 1 ? -1 : 1` で正しく反転している。
  各作品で「戻した直後(t=展開)に全頂点の z が 0 か」を実測して確認するとよい。
  ただし残差の傾きに依存して層順が成立している作品があるため、直すと見た目が
  変わる(うさぎでは❹の残差角の入れ替えが必要になった)。作品ごとに要検証。
- **(決定済み)くじらの色 = 現状維持。** 原典は完成形が紙の裏の色になる
  (❶を谷折りで始めるため)が、アプリは「動かない側が常に表を向く」方針で
  山折りに倒しているので表の色。2026-08-09 にユーザー判断で**この方針を優先**と
  決定(白い面が前に出る不具合を防ぐ方針を崩さない)。以後この件は再検討しない。

- **箱(box): 新規追加(2026-07-22)。アプリ初の立体作品。** ふたなしのトレイ型、
  全3工程: 上下のふちを90°立てる→左右のふちを90°立てる→四隅の耳(角の正方形を
  対角線で2分割)をとなりの壁に沿わせて内側へ折り込む。**アプリ初の angle 90 の
  立体折り**(それまでは全作品が ~170-180° の平畳み)。配色は枡らしい木色
  (外=濃い茶、内=薄い茶)、cameraAngle:35 で中が見えるよう見下ろす。
  **2026-08-03 修正: 四隅が「紙を切った」状態だったのを直した。** 旧版は
  角の正方形を対角線で分けた2枚を**別々の壁**へ折っていたが、1枚の紙では
  2枚は対角線でつながったままなので実物では起こらない。正しくは、壁を立てると
  角が自動的に対角線で二つ折りになり(2枚が稜まわりに ±45° 回って対角方向で
  出会う。頂点が (±0.854,±0.854,0.5) で完全一致=紙が切れていないことを実測確認)、
  その2枚重ねの耳をどちらか一方の壁へたたむ。❷に角のたたみ込み(±45°)を足し、
  ❸を「耳を左右の壁の内側へ 137°/139° 回してたたむ」に変更した。
  回す向きは「もう一方の壁を通り抜けない側」を選ぶこと(逆向きだと紙が壁を貫通する)。
  外側の角の頂点(±1,±1)は面ごとに複製(12〜19)して、壁を立てるとき
  もう片方の壁の面を引きずらないようにしてある。
  direction は auto 値からの相対ではなく**絶対指定**(回転軸 axis[0]→axis[1] の
  右ねじ)。この箱は軸が全て +z 向きなので、+z から見た反時計回りを正として
  設計できる。

## アーキテクチャ(詳細は README)

- 作品 = 工程データ(`src/models/*.ts`)。展開図頂点+面+工程列。動画・画像は持たない
- エンジン: `src/engine/fold.ts` — タイムライン位置tから毎フレーム全頂点をクォータニオン回転で計算。
  1工程に複数の折り(FoldOp)。工程内は連鎖適用(後のopの軸は前のopの移動後の位置)
- 折り種類: valley / mountain / inside-reverse(中割り=約180°回転で層が入替わる)/ outside-reverse。
  `direction: 1|-1` で回転符号を明示可(基本形のたたみ込み用)
- 面は折り線で必ず事前分割(`src/engine/split.ts` = エディタの面分割ツール)
- 描画: `src/three/PaperScene.ts`(React非依存)。UI: Navigator.tsx(ナビ画面)、Editor.tsx(工程データ作成ツール)
- 一覧カードの完成形プレビューは `CreasePattern.tsx` の `FinalShapePreview`
  (工程を最後まで適用して SVG に平行投影)。紙色は作品の `sheetColors` を使う
  (2026-08-09 以前は既定の薄い赤/白の決め打ちで、ハートが無色に見えていた)。
  z の幅が 0.2 を超える立体の作品(箱・コップ)はナビ画面と同じ視点から投影する
- **ナビ画面の工程一覧の折り図サムネイルは `buildStepDiagrams`(2026-08-12)**。
  同じ投影のしくみ(`viewFor` / `polygonsOf`)を `FinalShapePreview` と共有する。
  各工程を **折る前**(t=i。その工程を押したとき画面に出る状態)で描き、
  これから折る線を `FOLD_COLORS` / 折り図の破線で重ねる = 折り図と同じ見せ方。
  ・枠の中心は全工程で共通(紙が同じ場所で畳まれていくのが分かる)。ただし
    畳んだ形が小さくなりすぎる作品(手裏剣は2枚を離して置くので展開図が特に広い)
    のために、共通の縮尺の 2.2 倍までは工程ごとに寄る(中心は動かさないので切れない)
  ・面の境目(=事前分割の線)は細く薄く描く。完成形プレビューと同じ濃さだと
    44px では網目に見えて形が読めない
  ・**ナビ画面は毎フレーム再描画される**ので、Navigator 側で `useMemo` して
    要素をそのまま使い回す(同じ要素なら React が部分木の再描画を省く)。
    作品ごとに工程数ぶんの `computeFoldState` が走るため、毎フレーム作り直すと重い

## 作業ルール

- デザイン: 大人向けダーク和モダン(墨#0e0f12+朱#e0492f、Shippori Mincho / Space Grotesk)。
  絵文字・ポップな装飾は使わない。
- **言語は日英の切り替え式(2026-08-10。それまでは日英併記)**。`src/i18n.tsx` に
  `LangProvider` / `useLang()` / `LangToggle` があり、UI 文言も作品データと同じ
  `LocalizedText { ja, en }` で持つ。使い方は `const { t, L, lang } = useLang();` で
  `t('key')` が UI 文言、`L(model.name)` が作品データ。**新しい文言を足すときは
  i18n.tsx の DICT に ja/en 両方を書く**(片方だけにすると型エラーになる)。
  初期値は localStorage → `navigator.language` の順で決定し、`<html lang>` と
  `document.title` も追従する。
  小さな英字ラベル(ORIGAMI NAVIGATION / RANK / FOLDED / STEP / ROUTE)は
  デザイン上の記号として両言語で共通。日本語のときだけ添える英字(カードの英名・
  セクション見出しの SELECT A MODEL など)は `lang === 'ja'` で出し分ける
  (英語表示のときは同じ語が二重に出るため)
- **絞り込みは2軸(種類・難易度、AND)**(2026-08-12)。件数は「相手の絞り込みを
  適用した数」を出すので、押す前に結果が読める(例: 中級を選ぶと 種類 の件数が
  かお2/どうぶつ1/… に変わる)。相手の絞り込みで0件になったチップは押せない状態で
  残す(消すと画面が跳ねるため)。「しぼり込みを解除」は絞り込み中だけ出す。
- **ライブラリの分類と難易度ラベルは `src/catalog.ts`** に一覧で持つ(2026-08-10)。
  作品データ(models/*.ts)は幾何と工程に集中させ、見せ方の分類はここに置く方針。
  作品を足したら `CATEGORY_OF` に1行足す(足し忘れても 'other' 扱いで「すべて」には出る)。
  難易度は「工程数 + 層の扱いの難しさ」で 1=初級/2=中級/3=上級/5=名人 とし、
  ドット5個に加えて呼び名も出す(ドットだけでは差が伝わらない)。
  2026-08-10 に全作品を見直し: 初級7(チューリップ・犬・ねこ・きつね・ひよこ・
  ハート・正方基本形)/ 中級6(コップ・うさぎ・ぱんだ・くじら・兜・箱)/
  上級2(くま・手裏剣)/ 名人1(鶴)。2026-08-12 にヨット・ふね(初級)とぺんぎん・ろけっと(中級)を追加して全20作品。
  2026-08-13 に envelope(ふうとう、初級・つかう)と wallet(さいふ、初級・つかう)を追加して全22作品。
  2026-08-14 に turtle(かめ、中級・どうぶつ)を追加して全23作品。
  分類は7つ(かお・どうぶつ・のりもの・つかう・あそぶ・かざる・きほん)。
- **スマホの工程一覧は横スクロールの帯 `.route-rail`(2026-08-12)**。右のパネル
  (`.step-panel`)は 900px 未満では入らないので非表示のままで、代わりに折り図
  サムネイルだけを横に並べる(PCでは逆に帯を非表示)。工程が変わったら今の工程を
  中央へ寄せるが、`scrollIntoView` はページごと縦に動くことがあるので
  `scrollLeft` を直接指定する(`prefers-reduced-motion` のときは smooth を切る)。
  これで初回ガイドの「工程の一覧から、好きな工程へ飛べます」がスマホでも本当になった。
- **ナビ画面の初回ガイド**: 「3Dはドラッグで回せる/スライダーで途中に戻せる/
  工程一覧から飛べる」の3点を初回だけ出す(localStorage `origami-hint-seen`)。
  閉じた後もキャンバス右下の「?」から再表示できる。操作が伝わっていなかったため追加
- **キーボード操作**(2026-08-10): ← → で工程送り、スペースで再生/停止、
  Home/End で最初/完成形、Esc で一覧へ。入力欄にフォーカスがあるときは無効。
  ハンドラは ref 経由で最新の関数を呼び、useEffect の依存を増やさない。
  初回ガイドにも操作キーを載せている。
- **共有**(2026-08-10): 完成オーバーレイの「完成をシェア」は
  Web Share → クリップボード → **手動コピー用の入力欄**の順に段階的に落とす。
  クリップボードは権限で失敗することがあり、そのとき何も起きないと壊れて見えるため
  最後は文字列を出して選べるようにしている(実測: 権限拒否時に入力欄が出る)。
  index.html に OG/Twitter カードと theme-color も入れた(公開時のシェア見え)。
- **アクセシビリティ**: `:focus-visible` でキーボード操作時だけフォーカス枠を出す。
  `prefers-reduced-motion` の環境では transition/animation を実質無効にする。
- **開発時の注意**: `src/i18n.tsx` は React Context を作っているので、HMR で
  差し替わると既存ツリーが古い Context を参照して `useLang` が例外を投げ、
  画面が真っ暗になる。ファイル末尾で `import.meta.hot.invalidate()` して
  ページごと作り直すようにしてある(文言を1語足しただけで落ちるのを防ぐ)。
- 検証: `npx tsc -b` → `npm run dev` でブラウザ確認。ナビ画面では
  `window.__origami.setT(t)` でタイムラインを直接操作できる(E2E検証用フック)
- 幾何の検証: `node tools/solve-collapse.mjs`(基本形の符号・層順ソルバ)。
  新しい折りたたみも同様のソルバを tools/ に足して総当たりで解く
- 折り先が辺・折り線に重なる座標は厳密値で計算する(近似だとはみ出す。例: コップの s=2-√2)
- **作業が一段落したら必ずコミットして origin/main にプッシュする**(ユーザーの常時指示)
