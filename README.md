# おりがみナビ(プロトタイプ)

折り紙の折り方を3Dで順番にナビゲーションするスマホ向けWebアプリのプロトタイプ。
「折り紙版Google Maps」— 現在の工程・次の折り・完成までの残り工程を常に表示する。

## 起動

```bash
npm install
npm run dev
```

Node.js 24で回帰検証できます。

```bash
npm test
npm run build
npm run audit:models
npm run audit:references
```

2026-09-06の点検結果・修正内容・残存課題は
[33作品の完成形再照合](docs/final-shape-audit-2026-09-06.md)にまとめています。

ピザ・どんぐりを追加しました。鶴は花弁折りの途中変形を修正し、首・尾の細かな折りは簡略化しています。箱は簡易トレイです。

2026-09-08に、いえ・ちょうちょ・ソフトクリーム・すいか・たまごを追加しました。
工程と検証内容は [新作5点の追加](docs/new-models-2026-09-08.md)を参照してください。

全38作品で、折り目をつけるためだけの「折る→戻す」を省略しています。
左右や複数の角を折る操作も、一つずつ進められるように分割しました。
鶴18工程・手裏剣21工程・象4工程。袋を開く途中でも停止して確認できます。
最新の工程数と検証結果は [一工程ずつの案内](docs/individual-fold-stages-2026-09-06.md)を参照してください。
2026-09-07の [鶴9/11工程目の修正](docs/crane-petal-motion-2026-09-07.md)では、全18工程の面内距離・面積と紙の接続を検証しました。
工程一覧の選択は、その一工程を最後まで再生し、完了した工程番号で停止します。
鶴4で先端が重なる前に止まって見えた問題は [工程完了の表示](docs/step-completion-2026-09-07.md)で修正しました。

子プロセスの起動が制限される環境では、テストを次の方法で実行できます。

```bash
node --import ./tools/register-typescript.mjs --test --test-isolation=none tests/origami.test.mjs
```

## 現在できること

- 作品ライブラリ(鶴・手裏剣・動物の顔・のりもの・基本形など38作品)。
  サムネイルは工程データを最後まで適用した完成形をSVGで自動描画
  ([src/CreasePattern.tsx](src/CreasePattern.tsx))
- 工程データ駆動の3D折りシミュレーション
  (谷折り=シアン/山折り=ローズ/中割り折り=琥珀/かぶせ折り=菫)
  - 折り線の点線表示・折る面のハイライト・折る方向の3D矢印
  - 紙の表裏色分け(表=薄い赤/裏=白)
- 大人向けのダークUI(墨色ベース+朱アクセント、見出しは Shippori Mincho、
  英字は Space Grotesk、全面に紙質ノイズ)
- 全ラベル・工程説明の日英切り替え(データは `LocalizedText { ja, en }` で保持)
  - 「手前の1枚だけ折る」などレイヤーを区別した折り
- コマ送り操作:次へ/戻る/再生/工程スライダー/最初から/完成形
- 折っている間は折り線・矢印を表示し、完了後は同じ工程番号と完成した姿勢を保持
- ドラッグで3D回転、ピンチで拡大縮小、ダブルタップで正面に戻す
- 完成記録(localStorage)と称号(オリガミスト見習い〜神折り職人)

## アーキテクチャ

作品は動画や画像ではなく**工程データ**として持つ([src/engine/types.ts](src/engine/types.ts))。

- `vertices`: 展開図(平らな紙)上の2D座標。すべての折り線の端点を事前に埋め込む
- `faces`: 折り線で分割された面(多角形)
- `steps[]`: 1工程=複数の折り `folds[]`(折り線の頂点ペア+回転する頂点集合+山/谷+角度)
  +説明文。左右対称の折りは1工程に2本入れて同時に折れる(チューリップ工程2)

3D形状は [src/engine/fold.ts](src/engine/fold.ts) の `computeFoldState(model, t)` が
タイムライン位置 t(0=展開状態、k=工程kまで完了)から毎フレーム計算する。
工程を順に適用し、折り線の現在位置を軸に対象頂点をクォータニオン回転させる。
谷折り/山折りの回転方向は「谷=手前(+z)へ動く」から自動決定。
通常の平畳みは180°の剛体回転と独立した層の厚みで重なりを管理する。袋つぶしなどには近似が残る。

描画は [src/three/PaperScene.ts](src/three/PaperScene.ts)(three.js、React非依存)。
表と裏を別メッシュ(FrontSide/BackSide)で色分けし、面ごとの頂点色でハイライトする。

## 作品の追加方法

[src/models/dog.ts](src/models/dog.ts) を参考に:

1. 展開図に折り線をすべて書き、交点・端点を `vertices` に列挙する
2. 折り線で区切られた面を `faces` に列挙する(表から見て反時計回り)
3. 工程を順に `steps` に書く。各折りは折り線上の2頂点を `axis`、動く側の頂点を
   `moving` に指定。左右対称に同時に折るなら `folds` に2本入れる
4. `src/modelLibrary.ts` の `SOURCES` に追加。画面には折り目づけを省略した `MODELS` を使う

注意点:
- 後の工程で使う折り線も最初から `vertices`/`faces` に埋め込む(面の動的分割はしない)。
  折り線が面を横切る場合は必ずその線で面を分割しておくこと(斜めに曲がる欠陥が出る)
- 折り先が辺や折り線に「ぴったり重なる」座標は厳密に計算する
  (コップの s=2-√2 参照。近似だとはみ出しが目視でわかる)
- 同一工程内の複数折りは、互いの `axis` 頂点を動かさないこと

## 中割り折り・かぶせ折りの表現

ほぼ平らに畳まれた多層フラップの中割り折り(inside reverse)は、
「折り線を軸に対象領域の全レイヤーを約180°回転」で表現している
(レイヤーの前後が入れ替わるため、見える紙の色も実物と一致する)。
`FoldOp.type` に `'inside-reverse' | 'outside-reverse'` を指定し、
`sweep: 'front' | 'back'` で回す向きを選べる。「ことり」
([src/models/bird.ts](src/models/bird.ts))が実装例。
後工程の折り線は「完成状態で決めた線を、先行する折りで鏡映して展開図に埋め込む」
(bird.ts のコメント参照)。

## 工程データ作成ツール(β)

ライブラリ画面フッターの「MODEL EDITOR」から起動([src/Editor.tsx](src/Editor.tsx))。
「新規(正方形)」から作品を一から作れる。

- **折り線ツール**:展開図上で2点をクリック(頂点スナップ+数値微調整可)
  → その直線で全ての面を自動分割し、辺との交点に頂点を自動追加
  ([src/engine/split.ts](src/engine/split.ts))。既存頂点番号は保持される
- **工程ビルダー**:頂点クリックで「軸(2頂点)」「動く頂点」を指定し、
  折り種類(谷/山/中割り/かぶせ)・角度・回す向きを設定して工程を追加。
  1工程に複数の折り(同時折り)も組める。既存工程の削除・並べ替え対応
- **JSONタブ**:UIでの編集内容と双方向同期。直接編集して反映(検証つき)、
  コピーしてモデルファイル(src/models/)に貼り付けられる
- **3Dライブプレビュー**:タイムラインスライダーで任意の時点を確認
- 元に戻す(30段階)、既存4作品のプリセット読込

## 基本形のたたみ込み(単一頂点平坦折り)

正方基本形(square base)は「折り線(中心-頂点)を軸にした連鎖回転6回」で表現
([src/models/squareBase.ts](src/models/squareBase.ts))。厳密±180°では回転符号に
よらず同一の平坦形に畳まれ(tools/solve-collapse.mjs で検証)、符号は層の重なり順
(アコーディオンの向き)にだけ効くため、176°+交互方向を採用。
`FoldOp.direction` で回転符号を明示できる(山谷の自動判定のオーバーライド)。
折りが4本以上の工程は3D矢印を省略し折り線のみ表示、バッジは「たたむ COLLAPSE」。

## 実装メモ:鶴

三角2回 → 袋開き・つぶしを前後で実施 → 前後の花弁折り → 首・尾・頭 → 羽、の18工程です。
花弁折りは `petal` の距離拘束で紙をつなぎ、首・尾・頭は `foldFlap` で折り線を作ります。
鶴の物理座標は厚みゼロです。描画だけ `renderFaceOffsets` の微小な層間隔を適用します。
座標系は正方基本形と同じ -45° 回転。検証は `tests/origami.test.mjs` を参照してください。

## 2枚組み(モジュラー)の表現

手裏剣([src/models/shuriken.ts](src/models/shuriken.ts))はおりがみくらぶの折り図に忠実な
全9工程(観音折り→半分→端を斜め45°→端の帯を折り返して翼に→朱を裏返して90°回し中央へ→
藍を上にかさねる→差し込む→裏返す→差し込む)。展開図は0.5刻みの5×5格子+対角線の折り線で、
❹の折り線は❸の斜め辺と直角。両端に三角がついたユニットを作る。
正方形2枚(朱・鏡映した藍)を同一 `vertices`/`faces` に左右へ並べ、折ってから組み立てる。
「さしこむ」は三角の先端半分を中央へ約180°折る。`FoldOp.timing` で折りと層の移動を同期する。
組み立て用のエンジン拡張: `FoldOp.translate`(剛体平行移動)、
`FoldOp.spinZ`(面内回転。朱は裏返し180°+面内-45°、藍は面内45°で帯を縦横に合わせる)、
`FoldType 'assemble'`。シート別の色分けは `OrigamiModel.faceSheet`+`sheetColors`。
平らな作品は `cameraPos` で正面寄りから見せる。

手裏剣は `withFlatLayers` で各面の頂点を分離し、面全体を剛体として回す。
折り線上の紙端も層と一緒に動かし、束の最外層を蝶番とする厚み補正で上下の層順を反転する。
❹に0.02°の浅い折り残しを設け、差し込みの折り角と合わせて中央の交互の重なりを表す。
`mergeRigidPanels` は同じ動きをする面をまとめ、作図用の細かい格子を見せない。
裏面から始め、材質の色を途中で交換しない。ほかの作品には残差角による従来の近似が残る。

カメラは工程ごとに自動でフィットする([src/three/PaperScene.ts](src/three/PaperScene.ts) の
`aimFrame` / `easeFrame`)。いまの形が収まる最小距離と中心を折り状態が変わるたびに計算し、
そこへなめらかに寄せる。見る向き(`cameraAngle` / `cameraPos`)は変えないので、2枚が横に並ぶ
工程では引き、組み上がるにつれて寄る。上辺は折り種バッジと「正面」ボタンのぶん余白を厚くとり、
形の中心が余白の中央に来るよう注視点をずらす。ユーザーが3Dドラッグするとオートフィットは
止まり、「正面」またはダブルタップで戻る。

SVGとWebGLは `paperTriangles` を共有する。SVGでは交差する三角形をBSPで分割して描画順を決め、
平均奥行きのソートによる隠れた裏面の露出を防ぐ。完成見本は全作品で実モデルから生成する。

## 既知の制約
- 手裏剣の三角の折りと層順は再現するが、ポケット内部の曲面変形は紙厚と微小角で近似する
- 鶴の各面の辺長・面積は保持するが、首・尾の細幅化と中割り折りの内部の層移動は簡略化している
- かぶせ折りは色の入れ替わり(実物では裏の色が出る)を再現しない
- エディタはJSON直接編集+面分割(折り線の対話的作図UIは一部のみ)
- スワイプで次へ/戻る は未実装(3D回転ドラッグと競合するため保留)
- 工程ごとの推奨カメラ**角度**は未実装(寄り引きと注視点は工程ごとに自動で合わせる)
- オートフィットは紙の頂点だけを見るので、折り線の矢印が画面のふちを越えることがある
