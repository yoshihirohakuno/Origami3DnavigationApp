# 引き継ぎメモ(2026-08-15 時点)

次のセッション/別エージェントがそのまま作業を続けられるようにした状態記録。
**まず `CLAUDE.md` → `docs/verify-models.md` の順に読むこと。** このファイルは
「いま何が終わっていて、次に何をするか」だけを書く。

## 1. 現在の状態

- ブランチ `main`、origin と一致。作業ツリーはクリーン。
- 収録 **23作品**。分類は かお6 / どうぶつ5 / のりもの3 / つかう3 / あそぶ2 /
  かざる3 / きほん1。
- 既存22作品の原典照合は 2026-08-13 に完了(`docs/model-verification-report.md`)。
- **かめ(turtle)を追加して、前回の「進行中の作業」は完了**(2026-08-14、全5工程)。
  折り図の実測・工程・層順・色は `CLAUDE.md` のかめの項に全部書いてある。
- **「折り線をまたいだ面」のバグを3件修正**(てがみ 2026-08-14 / ひよこ・ねこ 2026-08-15)。
  どれも「折り目をつけるだけの工程の折り線で面を分割していない/moving に入れ忘れた」もの。
  **辺の長さの点検**(`CLAUDE.md`「辺の長さで『取り残された頂点』を自動検出」)で検出し、
  現在は鶴(中割り折り=エンジンの仕様)以外の全22作品が 0.05 未満。
- ビルド・lint は通る(`npx tsc -b` / `npx oxlint src` は既存の react 警告のみ)。

## 2. 次にやること(優先度順)

### A. 保留中の2作品(手を出す前に読む)

`CLAUDE.md` の「保留中の課題」に実測値つきで残してある。

- **さかな2**(easy/sea/fish2): ❺のパネルの輪郭が❹の結果と合わない。
- **えんばん**(easy/vehicle/ufo): ❹の脚は「中線と斜線ではさまれた三角を斜線で
  折り返した形」と完成図がピクセル一致するが、その三角は中線側が折り目なので
  そのまま回すと紙が切れる。
- どちらも**折り図の破線が自由なフラップを区切っていない**タイプ。1軸の回転しか
  持たない現エンジンでは、層の読みを詰めるかエンジン拡張(スワイベル)が要る。

### B. 新しい作品を足す

かめ(2026-08-15 完了)で、折り図から作れる素直な作品はひととおり入った。次に足すなら
おりがみくらぶで**折り図があり、1軸の回転だけで畳める**作品を選ぶこと(§A の2つは
その条件を満たさないので保留にしてある)。手順は §4 と `docs/verify-models.md`。

## 3. 決定済み(蒸し返さない)

- **鶴**: 原典 traditional/crane は「三角2回→袋開き」、アプリは折りすじ先行の
  collapse。**現行維持**(2026-08-13 ユーザー判断)。同じ正方基本形に到達する。
- **箱**: 原典 traditional/box は「はこ1」、アプリはふたなしトレイ(伝承)。
  **現行維持**。別作品である旨を明記して残す。
- **くじらの色** / **手裏剣の中央の白のぞき** / **ヨットの 7.3° の傾き** は既定どおり。
- **原典のない作品は置かない**(ことり→ひよこ、ふうとう→てがみ、さいふ→ぴあの)。
  新規は必ず折り図のある作品から作る。
- **かめの❷の折り線 C=0.42** は折り図に目印がない実測値。3パネルの平均で決めた
  ので、これ以上の追い込みはしない。

## 4. 作業のやり方(最低限)

```bash
npx tsc -b && npm run dev            # 型 → 起動
node tools/zu-measure.mjs fetch <名> # 折り図の取得(画像名は zu.html から)
```

- 画面の検証は `window.__origami.setT(t)` / `getPositions()` / `getModel()`。
  **「角が○○に来る」という見本の目印は必ず数値で確認する。**
- 新しい作品/折りを足したら、**辺の長さの点検**(§2A)を必ず1回通す。
  折り線の向こう側の頂点を `moving` に入れ忘れる事故がこれで消える。
- 完成形の縦横比を折り図の完成図(`zu-measure.mjs bbox`)と比べる。
- 新しい作品を足したら `src/App.tsx` の MODELS と `src/catalog.ts` の CATEGORY_OF、
  `CLAUDE.md` の作品項目、`docs/verify-models.md` の表を更新する。
- 1作品 = 1コミット。何が見本と違ったか/どう直したかを書く。
