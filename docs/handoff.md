# 引き継ぎメモ

## 2026-09-06 最新: 折り目づけ工程を省略

ユーザー指定により全33作品の表示ルートを確認し、22作品の「折る→戻す」計60工程を省略した。
鶴7工程、象3工程、どんぐり4工程。詳細は [工程省略](crease-preparation-omission.md)。
共通入口は `src/modelLibrary.ts`。元データの工程番号は層の計算用に維持している。
残る工程の形状・途中の動き・表裏は変更前と一致。103テストとビルドを確認。
鶴の形状再検証中・箱の簡易トレイという既知の課題は継続。以下の工程数は過去の記録。

> 最新の全33作品の結果は [完成形再照合](final-shape-audit-2026-09-06.md)。以下は過去の記録を含む。

## 2026-09-06 更新

品質修正と全31作品の数値点検を実施。最新の結果は `quality-audit-2026-09-06.md`、
実装方針は `CLAUDE.md` 冒頭を参照。手裏剣の白面スタート・多層折り・差し込み、
コップの口開き、SVGの表裏と重なり順を修正した。`npm test` で39件を検証。
手裏剣❹は❸と直角、差し込みは先端の三角半分を約180°折る。旧「平行」「344°」は誤り。
鶴・基本形などの袋つぶしの途中で辺長が変わる問題は残存課題として数値を記録。
以下は過去の記録。

## 2026-08-24 時点

次のセッション/別エージェントがそのまま作業を続けられるようにした状態記録。
**まず `CLAUDE.md` → `docs/verify-models.md` の順に読むこと。** このファイルは
「いま何が終わっていて、次に何をするか」だけを書く。

## 1. 現在の状態

- ブランチ `main`、origin と一致。作業ツリーはクリーン。
- 収録 **31作品**。分類は かお6 / どうぶつ7 / のりもの6 / つかう4 / あそぶ2 /
  かざる3 / たべもの1 / きほん2。
- **くるまを追加(2026-08-26、全10工程)**。折り線がすべて目印つき。
- **作品追加のループ(実装役↔レビュー役)を導入(2026-08-26)**。手順は `docs/model-loop.md`。
  1周目(くるま)で**レビュー役が本物のバグを1件見つけた**: ❶の戻しの符号が逆で、
  開いても紙が 4° 傾いたまま残っていた(他28作品は 0)。レビュー役に
  「実装役の数値を出発点にしない」を守らせるのが効いている。
- **おたまじゃくしを追加(2026-08-25)**。❶〜❸は正方基本形の鏡写し(squash の連鎖回転を
  そのまま流用できた)。尾の2層の層順は複製頂点+残差角 178/172 で作る。
- **おむすびを追加(2026-08-25)**。分類「たべもの」を新設。折り図の実測は CLAUDE.md に。
- **水風船基本形を追加(2026-08-24)**。原典 fun/balloon の❶〜❻、全5工程。
  ふうせん・かえるの土台になる基本形で、正方基本形と対になる(中線で2回折る)。
  色の出方は折り図と一致(`100 0 0 0 0 0`)。
- **うらがえしたあとの squash のバグを2件修正(2026-08-24)**。水風船基本形❺と
  **正方基本形❺**。どちらも「2本目の軸に1本目で浮いた頂点を使っていた」+
  「うらがえしで束が -z 側へ移るのに自動符号のまま」で、完成形の外側だけ
  裏の白が出ていた。詳細は `CLAUDE.md`「うらがえしたあとの squash は、軸と符号を明示する」。
- **正方基本形を白面スタートにした(2026-08-24、ユーザー判断)**。❺を直すと色面スタートでは
  完成形が白くなる(物理的に正しい)ため、実物と同じ「色を出したいなら白い面から折り始める」
  に合わせた。裏面% は `100 0 0 0 0 1` で水風船基本形と同じ出方。
- 既存22作品の原典照合は 2026-08-13 に完了(`docs/model-verification-report.md`)。
- **かめ(2026-08-14)/ しんかんせん・ぶーつ(2026-08-15)**を追加。
  折り図の実測・工程・層順・色は `CLAUDE.md` の各項に書いてある。
- **表裏・山谷の総点検を実施(2026-08-15)。4件直した**: ぴあの❺(山→谷)/
  ろけっと(表裏の初期向き)/ コップ・兜(多層フラップの層順)。
  点検の道具は **`tools/audit-cover.mjs`**(工程ごとの裏面%を全作品ぶん出す)。
  ノウハウ「2枚重ねのフラップは折るたびに前後が入れ替わる」は `CLAUDE.md` に記載。
- **正方基本形を原典どおりの5工程にした(2026-08-15)**。三角2回→袋開き2回。
  **袋を開いてつぶす(squash)は1軸回転2回の連鎖で書ける**ことが分かった(`CLAUDE.md`)。
  「折り目をつけて戻す」を独自に足していたのは正方基本形と**鶴**だけで、
  他の作品はすべて折り図にその工程がある(2026-08-15 に折り図で再確認)。
- **「折り線をまたいだ面」のバグを3件修正**(てがみ 2026-08-14 / ひよこ・ねこ 2026-08-15)。
  どれも「折り目をつけるだけの工程の折り線で面を分割していない/moving に入れ忘れた」もの。
  **辺の長さの点検**(`CLAUDE.md`「辺の長さで『取り残された頂点』を自動検出」)で検出し、
  現在は鶴(中割り折り=エンジンの仕様)以外の全22作品が 0.05 未満。
- ビルド・lint は通る(`npx tsc -b` / `npx oxlint src` は既存の react 警告のみ)。

## 2. 次にやること(優先度順)



### A. 保留中の2作品(手を出す前に読む)

`CLAUDE.md` の「保留中の課題」に実測値つきで残してある。

- **さかな2**(easy/sea/fish2): ❺のパネルの輪郭が❹の結果と合わない。
- **えんばん**(easy/vehicle/ufo): ❹の脚は「中線と斜線ではさまれた三角を斜線で
  折り返した形」と完成図がピクセル一致するが、その三角は中線側が折り目なので
  そのまま回すと紙が切れる。
- どちらも**折り図の破線が自由なフラップを区切っていない**タイプ。1軸の回転しか
  持たない現エンジンでは、層の読みを詰めるかエンジン拡張(スワイベル)が要る。

### A-2. 鶴を原典ルートへ移すか(ユーザー要望・未着手)

2026-08-15 に「折り目をつけて戻すのは要らないのでは」という指摘があり、
正方基本形は原典ルート(三角2回→袋開き2回)へ移した。**鶴だけ独自ルートのまま**。
移すなら:

- 正方基本形と同じ squash(1軸回転2回の連鎖)で鶴の❶〜❾を5工程に置き換える
- 鶴の展開図は頂点20個あるので、5工程ぶんの moving 集合を作り直す
- **花弁折り以降(❿〜⓯)は工程9の層順に乗っているので、置き換え後の層順が
  同じかを `tools/audit-cover.mjs` と辺の長さ点検で確かめること**。違えば
  ❿以降も作り直しになる
- 2026-08-13 の「鶴は現行維持」の決定は、この指摘で見直し対象になっている

### B. 次に足す作品(折り図の読みは済み。実装だけ残っている)

2026-08-15 に候補を洗い、**しんかんせん**(24作品目)と**ぶーつ**(25作品目)は
実装まで完了。残りは折り図を実測して工程まで読んであるので、そのまま実装に入れる。

- **さかな(easy/sea/fish、全9パネル)**: 2026-08-25 に❶〜❸まで実測して確定した。
  紙はひし形、白い面を上に始める(表=青 / 裏=白)。
  ・❶ よこ半分に折る(上の角を下の角へ)。三角になり全面が青
  ・❷ **手前の1枚だけ**を **y=-h(h≈0.38)** で折り上げる。折り上げた先は上のふちより
    上に出て(y=1-2h=0.24)、白い三角になる。**残る青は左右の2つの三角**(折り図❸と一致)。
    h は目印のない実測値: ❷の破線 0.381 / ❸の青い三角の先 0.382 / ❹の青の消える高さ 0.385
  ・❸ **左の角 A=(-(1-h),-h)(❷の折り線が紙の左のふちと交わる点)から 22.5°下がりの線**で、
    下の角 S(0,-1) を折り上げる。S は A と同じ高さの **((√2-1)(1-h), -h)** に来て、
    紙の右下のふちが**垂直**になる(折り図❹の青いフラップの右のふちと一致)。
    折り線は A から 紙の右下のふちの **((√2-1)(1-h), (√2-1)(1-h)-1)** まで(実測 (0.26,-0.74))
  ・**❹(2026-08-26 に読んだ)**: ❸でできた青いフラップ(直角三角形 A(-(1-h),-h) /
    S'((√2-1)(1-h),-h) / B((√2-1)(1-h),(√2-1)(1-h)-1))の**上のふち側**を、
    **❸の折り線と平行な線(-22.5°)**で下へ折る。折り線が上のふちを切る x は約 **-0.48**
    (❹の破線から -0.50 / ❺の結果から逆算して -0.464)。S' は **(0.04,-0.89)** へ回り
    (折り図❺の最下点の実測 (0.005,-0.89))、青い先 B はそのまま残る = 折り図❺の青い先。
    折った紙のふちは 45°(紙のふちと平行)になる
  ・**❺(同)**: 左の耳を通る折り線 **(-0.596,0)-(-0.46,-0.45)**(傾き dx/dy=-0.30)で
    左側を右へ折る。目印なしの実測
  ・**❻(2026-08-26 に読んだ)**: ❺で立った左のひれを、**45°の線**
    (-0.558,-0.022)-(-0.355,-0.232) で左へ倒す(矢印は左向き)。目印なしの実測
  ・**❼❽**: ❼は右上の耳の角をうしろへ折る(折り図❼の破線と輪の矢印)、❽はうらがえし。
    ❽でうらがえすと全面が青になる(折り図❾)
  ・**保留の判断(2026-08-26)**: ❺❻がどちらも「多層のひれの中の、目印がない折り線」で、
    パネルが小さく破線を1本に確定できない(青の塗りと破線の明度が近く、無彩色判定で
    分けても複数の線が残る)。ユーザー方針(読み切れないものは後回し)に従って次の候補へ。
    再開するなら、まず❺❻を**実物で折って**ひれの層を確かめるのが早い
  ・パネルの較正は「最大幅の行=紙のふちの高さ」で取る。**青は有彩色なので
    `isColor` で拾える**(おむすびの灰色と違って色面判定が効く)。

- ~~**ばす**~~ → **2026-08-26 に実装完了(全8工程)**。以下は読みの記録。
  紙は [-1,1] の正方形、**白い面を上にして始める**(表=橙 / 裏=白)。
  ❶よこ半分の折り目(折って戻す)→ ❷上下のふちを **y=±2/3** で内へ →
  ❸帯の四すみを対角線で外へ折る → ❹四すみの先を小さく折り下げる →
  ❺よこ半分に折る → ❻上の両角を中割り折り → ❼まどを描いてできあがり。
  ・**❷の折り線 y=±2/3** は3つの測り方が一致(折り図❸: 帯の高さ 0.31 / 全高 1.31 /
    白い中央 0.69 → いずれも h≈0.67)。折った帯のふちが **y=±1/3** に来て、
    白い中央がちょうど 2/3 になる。折り図❷の破線(±0.37)は**折ったふちの着地線**で、
    折り線そのものではない(破線どおりに折ると帯が中央で重なってしまう)
  ・**❸の折り線は帯の対角線 (∓1,±2/3)-(∓1/3,±1)**(展開図の座標。折った状態では
    (∓1,±2/3)-(∓1/3,±1/3))。紙の四すみ (∓1,±1) は **(∓0.733,±0.867)** へ回って
    帯の外へ出る。折り図❹の実測(ピーク (∓0.74,±0.92)・輪郭が45°で狭まる)と一致
  ・**❹はピークの先を小さく折り下げるだけ**(折り図❹の小さな下向き矢印と短い破線)。
    折り線は折った状態で **先から 0.065 下の水平線**、展開図では紙の角を切る
    **(∓1,±0.892)-(∓0.919,±1)**(❸→❷の鏡映を逆にたどって逆算)
  ・**❺は y=0**(❶の折り目)。折り図❻の本体が **2/3 の高さ**(実測 0.664)で、
    車輪の先が上のふちから **0.86**(予測 0.867)なのが裏づけ
  ・**❻は上の両角の中割り折り**。折り線は折り後の **(∓0.90,0)-(∓1,∓0.06)**(実測)。
    折り図❻の拡大図が層の入り方を示している
  ・車輪は「❸で外へ出た三角」が❺で下へ回ったもの。折り図❻の車輪の上端の幅
    (-0.976..-0.557)は、この三角の2辺 (∓1,∓2/3)-(∓0.733,∓0.867) と
    (∓0.733,∓0.867)-(∓1/3,∓1/3) で説明できる(予測 -0.586、実測 -0.557)

- ~~**ぞう**~~ → **2026-08-26 に実装完了(全7工程)**。以下は読みの記録。
  紙は [-1,1] の正方形、**白い面を上にして始める**(表=水色 / 裏=白)。
  ❶たてよこの折り目(折って戻す)→ ❷左を斜めの線で右へ折る → ❸よこ半分に折る →
  ❹左下から袋を開いてつぶす(頭と鼻)→ ❺かおを描いてできあがり。
  ・**❷の折り線は (-0.5,1)-(-1,-1)**(上のふちの「左のふちと中心の折り目の中点」から
    左下の角へ。傾き 1/4)。2パネルで裏が取れる: 折り図❷の破線の外挿が (-0.594,1)-(-0.947,-1)、
    折り図❸の青い部分の左のふち(=折り線)が (-0.49,1)-(-0.884,-1)。どちらも傾き 0.24〜0.25
  ・この折りで**左上の角 (-1,1) が (-0.06,0.76) へ回る**(折り図❸の青い部分の右端の実測
    (0.01,0.83)。折り線からの距離 0.5 が保存されるのも確認済み)
  ・❸は y=0(❶の折り目)で上半分を下へ。結果は 2×1 の横長になる
  ・**❹は袋つぶし**。左下の角から右上へ向かう斜めの破線で、⇧の矢印は下から袋へ入る。
    正方基本形・水風船基本形・おたまじゃくしと同じ「1軸回転2回の連鎖」で書けるはず
  ・完成形(折り図❺)は、体が横長の長方形、右に頭と目、下に鼻が垂れ、左下に足の切れこみ

- **しゃつ(easy/clothes/shirt、全8パネル)— 2026-08-26 に読んで保留**。
  ❶たてよこの折り目 → ❷上のふちを**うしろへ**(帯は紙の高さの約10.4% = 展開図で y=0.8)→
  ❸観音折り(x=±0.5)→ ❹白い帯の中に、上の両角から中心へのV字の折り目をつけて戻す →
  ❺**づらす**(帯の左右を下へずらして襟にする。拡大図が2つ付いている)→
  ❻下の両角を斜めに折り上げてそで → ❼下を折り上げてすそを襟に差し込む → ❽できあがり。
  **保留の理由(❹の色が説明できない)**: ❷で帯を**うしろへ**折るなら帯は胴の後ろにあり、
  ❸の観音折りは前に来るので、❹の上端は緑になるはず。ところが折り図❹は
  **上に白い帯(高さ18px ≒ 0.2単位)があり、その下が緑**。帯が観音折りより前に見える
  層の順が読めない。加えて❺「づらす」は折りではなく層のずらしで、エンジンでは
  `translate` の近似になる。**実物を折って層を確かめてから作ること**。
  実測メモ: ❹のパネルは幅88px(=観音折り後の幅=1単位)、白帯18px、緑283..439px、
  ❷の破線は紙の上から10.4%。ユーザー判断(2026-08-26)により、読めない作品は
  後回しにして次の候補へ進む方針。

## 3. 決定済み(蒸し返さない)

- **鶴**: 原典 traditional/crane は「三角2回→袋開き」、アプリは折りすじ先行の
  collapse。**現行維持**(2026-08-13 ユーザー判断)。同じ正方基本形に到達する。
- **箱**: 原典 traditional/box は「はこ1」、アプリはふたなしトレイ(伝承)。
  **現行維持**。別作品である旨を明記して残す。
- **くじらの色** / **手裏剣の中央の白のぞき** / **ヨットの 7.3° の傾き** は既定どおり。
- **原典のない作品は置かない**(ことり→ひよこ、ふうとう→てがみ、さいふ→ぴあの)。
  新規は必ず折り図のある作品から作る。
- **かめの❷の折り線 C=0.42** は折り図に目印がない実測値。3パネルの平均で決めた
  ので、これ以上の追い込みはしない。

## 4. 作業のやり方(最低限)

```bash
npx tsc -b && npm run dev            # 型 → 起動
node tools/zu-measure.mjs fetch <名> # 折り図の取得(画像名は zu.html から)
```

- 画面の検証は `window.__origami.setT(t)` / `getPositions()` / `getModel()`。
  **「角が○○に来る」という見本の目印は必ず数値で確認する。**
- 新しい作品/折りを足したら、**辺の長さの点検**(`CLAUDE.md` の同名の節)と
  **`tools/audit-cover.mjs` の表裏点検**を必ず1回ずつ通す。
  折り線の向こう側の頂点を `moving` に入れ忘れる事故がこれで消える。
- 完成形の縦横比を折り図の完成図(`zu-measure.mjs bbox`)と比べる。
- 新しい作品を足したら `src/App.tsx` の MODELS と `src/catalog.ts` の CATEGORY_OF、
  `CLAUDE.md` の作品項目、`docs/verify-models.md` の表を更新する。
- 1作品 = 1コミット。何が見本と違ったか/どう直したかを書く。
